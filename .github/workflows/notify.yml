name: Daily Push Notification

on:
  schedule:
    - cron: '30 6 * * *'  # UTC 06:30 = TR 09:30
  workflow_dispatch:  # Manuel test için

jobs:
  send-notification:
    runs-on: ubuntu-latest
    steps:
      - name: Send OneSignal Push
        run: |
          curl -X POST "https://onesignal.com/api/v1/notifications" \
          -H "Authorization: Basic ${{ secrets.ONESIGNAL_API_KEY }}" \
          -H "Content-Type: application/json" \
          -d '{
            "app_id": "${{ secrets.ONESIGNAL_APP_ID }}",
            "included_segments": ["All"],
            "headings": {"tr": "Günaydın! Kelime Yolculuğu Başlıyor 🌍"},
            "contents": {"tr": "Zihnini tazelemeye hazır mısın? Lexatlas'\''ta bugünün kelimeleri seni bekliyor. Hemen gel, keşfetmeye başla!"}
          }'
